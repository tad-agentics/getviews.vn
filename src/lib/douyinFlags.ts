/**
 * Flip to ``false`` when the Douyin ingest + cron pipeline is live and Kho
 * Douyin should load normally. (Remove the gate in product once stable.)
 */
export const KHO_DOUYIN_COMING_SOON = false;
