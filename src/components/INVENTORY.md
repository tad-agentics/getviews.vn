# `src/components/` inventory (feature agents)

- **`ui/`** — Figma Make + shadcn-style primitives (Radix). Full list: `artifacts/docs/design-system-spec.md`.
- **`AppLayout.tsx`** — Main app chrome (mock sessions/profile until wired).
- **`BottomNav.tsx`** — Mobile bottom navigation (Chat / Xu hướng / Lịch sử).
- **`EmptyState.tsx`**, **`ErrorBanner.tsx`**, **`SkeletonCard.tsx`** — Standard non-happy paths.
- **`figma/ImageWithFallback.tsx`** — Image helper.
