/** DataFreshnessPill — removed per UX cleanup. Kept as a no-op so import sites compile. */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function DataFreshnessPill(_props: { asOfIso?: string | null }) {
  return null;
}
