/**
 * Phase A · A3 — redesign primitives (Getviews Studio v2 design system).
 *
 * Tokens defined in src/app.css under --gv-*. Import from this barrel so
 * the call sites don't drift:
 *
 *   import { Kicker, SectionHeader, Chip, Btn, Card, Composer,
 *            Segmented, Bignum, TickerMarquee } from "@/components/v2";
 *
 * Everything here is display/layout only — no domain data, no fetching.
 */

export { Kicker } from "./Kicker";
export { SectionHeader } from "./SectionHeader";
export { Chip } from "./Chip";
export { Btn } from "./Btn";
export { Card } from "./Card";
export { Composer } from "./Composer";
export { Segmented } from "./Segmented";
export { Bignum } from "./Bignum";
