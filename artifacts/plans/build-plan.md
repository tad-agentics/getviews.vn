# Build Plan — GetViews.vn

> Generated by the Tech Lead during `/setup` on 2026-04-09. Self-contained context packages for each agent dispatch.

---

## Feature Dependency Graph

| Wave | Feature | Backend scope | Frontend scope | Depends on |
|---|---|---|---|---|
| Foundation | shared-infra | Supabase schema (14 tables), RLS, triggers, RPCs, Edge Functions (payos-webhook, create-payment, send-email, 5 cron functions), SEO static files | AppLayout, BottomNav, auth screens (LoginScreen, callback), landing page (`/`), useInstallPrompt, shared hooks (useAuth, useProfile, useNicheTaxonomy), api-types.ts | — |
| Wave 1 | auth | handle_new_user trigger, profiles RLS, anonymous_usage table | LoginScreen Facebook+Google OAuth flow, auth guard layout (`_app/layout.tsx`), niche inline first-session flow | Foundation |
| Wave 1 | chat-core | chat_sessions + chat_messages tables + RLS, decrement_credit RPC, is_processing guard, Vercel `/api/chat` Edge route (text intents ⑤⑥⑦), Cloud Run `/stream` SSE endpoint wiring | ChatScreen — all 7 intents, streaming SSE, DiagnosisRows, HookRankingBars, BriefBlock, CreatorCards, ThumbnailStrip, CreditBar, PromptCards, D1–D6 dopamine | auth |
| Wave 1 | history | — (reads chat_sessions + chat_messages) | HistoryScreen — session list, search, resume | chat-core |
| Wave 2 | explore | video_corpus table + RLS (Explore SELECT), useVideoCorpus + useVideoDetail hooks | ExploreScreen — 2-col grid, niche/date/sort filters, VideoDetailModal (inline video player, similar videos, "Phân tích" CTA) | auth |
| Wave 2 | trends | niche_intelligence materialized view + RLS, trend_velocity + hook_effectiveness + format_lifecycle + creator_velocity tables + RLS, useNicheIntelligence + useTrendVelocity + useHookEffectiveness + useFormatLifecycle hooks | TrendScreen — NicheSelector, HookRankingBars (D2), FormatLifecycleSection, TrendingKeywordSection, ThumbnailStrip | auth |
| Wave 2 | billing | subscriptions + credit_transactions + processed_webhook_events tables + RLS, create-payment Edge Function, payos-webhook Edge Function, send-email Edge Function (receipt), useSubscription hook | PricingScreen, CheckoutScreen, PaymentSuccessScreen | auth, chat-core (CreditBar) |
| Wave 2 | settings | updateProfile mutation, credit_transactions SELECT | SettingsScreen — profile, subscription info, niche inline change, credit history, logout, LearnMoreScreen link | auth, billing |
| Wave 2 | email-cron | cron-expiry-check, cron-reset-free-queries, cron-prune-webhooks, cron-reset-processing Edge Functions; email templates (expiry_reminder_7d, expiry_reminder_3d, expiry_reminder_1d) | — (backend only) | billing |

---

## Foundation Context Package

### What Foundation delivers

1. **Backend:**
   - All 14 Supabase table migrations + RLS policies + indexes
   - `handle_new_user` trigger (creates `profiles` row on `auth.users` INSERT)
   - `decrement_credit` RPC function
   - All Edge Functions scaffolded (with correct Deno.serve patterns and HMAC/JWT verification stubs)
   - SEO static files: `public/robots.txt`, `public/sitemap.xml`, `public/manifest.json` (already exists — verify)

2. **Frontend:**
   - `src/lib/supabase.ts` — single client instance
   - `src/lib/auth.tsx` — AuthProvider + useAuth hook
   - `src/lib/api-types.ts` — all TypeScript interfaces (copy from tech-spec §4)
   - `src/hooks/useProfile.ts`, `useNicheTaxonomy.ts`
   - `src/hooks/useInstallPrompt.ts` — beforeinstallprompt + iOS fallback
   - `src/routes/_auth/login/route.tsx` — LoginScreen (from Make: `LoginScreen.tsx`)
   - `src/routes/_auth/callback/route.tsx` — OAuth callback handler
   - `src/routes/_app/layout.tsx` — auth guard layout
   - `src/routes/_index/route.tsx` — LandingPage (from Make: `LandingPage.tsx`)
   - `src/app.css` — already synced with Make design tokens ✓
   - AppLayout + BottomNav shared components (from Make: `AppLayout.tsx`)

### Make files to port in Foundation

All Make files are under `src/make-import/` (git-ignored). Full paths shown below.

| Make file (under `src/make-import/`) | Destination route/component |
|---|---|
| `src/app/screens/LandingPage.tsx` | `src/routes/_index/route.tsx` |
| `src/app/screens/LoginScreen.tsx` | `src/routes/_auth/login/route.tsx` |
| `src/app/components/AppLayout.tsx` | `src/components/AppLayout.tsx` |
| `src/app/components/ui/*` | `src/components/ui/*` (as-is) |

### Supabase migrations to create (Foundation)

File naming: `supabase/migrations/YYYYMMDDHHMMSS_<name>.sql`

1. `001_niche_taxonomy.sql` — niche_taxonomy table
2. `002_profiles.sql` — profiles table + RLS + handle_new_user trigger
3. `003_chat.sql` — chat_sessions + chat_messages tables + RLS + indexes
4. `004_subscriptions.sql` — subscriptions + credit_transactions + processed_webhook_events + decrement_credit RPC
5. `005_corpus.sql` — video_corpus + niche_intelligence (materialized view) + trend_velocity + hook_effectiveness + format_lifecycle + creator_velocity + batch_failures tables + RLS
6. `006_anonymous.sql` — anonymous_usage table

### Edge Functions to scaffold (Foundation)

| Function | Trigger | Key operation |
|---|---|---|
| `payos-webhook` | PayOS HTTP POST | HMAC verify → idempotency check → credit grant RPC → email |
| `create-payment` | Client `supabase.functions.invoke()` | PayOS REST API call → return QR + bank details |
| `send-email` | Internal (from other functions) | Resend `emails.send()` |
| `cron-expiry-check` | pg_cron `0 2 * * *` UTC | Expire subscriptions, send reminder emails, downgrade tiers |
| `cron-reset-free-queries` | pg_cron `0 17 * * *` UTC | `UPDATE profiles SET daily_free_query_count = 0` |
| `cron-prune-webhooks` | pg_cron `0 20 * * 0` UTC | Delete processed_webhook_events older than 30 days |
| `cron-reset-processing` | pg_cron `*/5 * * * *` UTC | Reset stale `is_processing = true` flags |

### Environment variables needed (Foundation)

**Vercel (fill before `npm run dev`):**
```
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_CLOUD_RUN_API_URL=
```

**Supabase Edge Function secrets (set via `supabase secrets set`):**
```
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
PAYOS_API_KEY=
PAYOS_CLIENT_ID=
PAYOS_CHECKSUM_KEY=
RESEND_API_KEY=
```

**Cloud Run env vars (set in GCP Cloud Run service config — NOT in Vercel):**
```
ENSEMBLE_DATA_API_KEY=
GEMINI_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_ACCOUNT_ID=
R2_BUCKET=getviews-videos
R2_PUBLIC_URL=
```
See `cloud-run/.env.example` for the full list. Cloud Run secrets are managed in GCP, not Vercel.

### Acceptance criteria — Foundation

- [ ] `npm run dev` runs without errors
- [ ] `/` renders LandingPage (pre-rendered HTML visible in view-source)
- [ ] `/login` renders LoginScreen with Facebook + Google buttons
- [ ] Supabase migrations apply cleanly: `supabase db reset` completes without errors
- [ ] RLS test: anon key cannot read `profiles` without matching `auth.uid()`
- [ ] `useInstallPrompt` hook captures `beforeinstallprompt` in Chrome dev tools

---

## Wave 1 — Feature Context Packages

### Feature: auth

**Backend scope:**
- `handle_new_user` trigger (already in Foundation migration 002)
- `anonymous_usage` table + RLS (Foundation migration 006)
- FR-01 anonymous free Soi Kênh: Cloud Run validates JWT or skips if anonymous; `anonymous_usage` tracks IP hash

**Frontend scope:**
- `src/routes/_auth/login/route.tsx` — port `LoginScreen.tsx` from Make
- `src/routes/_app/layout.tsx` — auth guard; if no session → redirect to `/login`; check `anonymous_usage.has_used_free_soikenh` for free trial gating
- Niche selection inline: ChatScreen first session shows NicheSelector if `profiles.primary_niche IS NULL`
- `useAuth` hook: `supabase.auth.signInWithOAuth({ provider: 'facebook' | 'google' })`
- After OAuth callback: `profiles.primary_niche` null check → NicheSelector inline in ChatScreen

**Make files to port** (from `src/make-import/src/app/screens/`)**:**
- `LoginScreen.tsx` → `src/routes/_auth/login/route.tsx`

**Copy slots (production-ready):**
- `trust_line`: "Data thực từ 46.000+ video TikTok Việt Nam — phân tích video của bạn trong 1 phút."
- `btn_facebook`: "Đăng nhập với Facebook"
- `btn_google`: "Đăng nhập với Google"
- `loading_facebook`: "Đang kết nối Facebook..."
- `error_oauth`: "Đăng nhập không thành công — thử lại."
- `legal_note`: "Bằng cách đăng nhập, bạn đồng ý với Điều khoản dịch vụ và Chính sách bảo mật của GetViews."

**Edge cases:**
- Facebook OAuth blocked by corporate browser → "Thử đăng nhập bằng Google hoặc mở trong Safari/Chrome."
- User returns to `/login` already logged in → auto-redirect to `/app`

**Credit cost:** N/A

**Acceptance criteria:**
- [ ] Facebook OAuth completes and creates `profiles` row (verify in Supabase Dashboard)
- [ ] Google OAuth completes and creates `profiles` row
- [ ] New user lands on ChatScreen after callback
- [ ] Returning user skips LoginScreen when navigating to `/app`
- [ ] RLS: user A cannot read user B's profile

---

### Feature: chat-core

**Backend scope:**
- `chat_sessions` + `chat_messages` + RLS (Foundation migration 003)
- `decrement_credit` RPC (Foundation migration 004)
- `profiles.is_processing` guard (Foundation migration 002)
- **Vercel Edge Runtime** — `api/chat.ts` (project root `api/` directory, NOT inside `src/`): handles text intents ⑤⑥⑦ + follow-ups. Validates JWT, checks credits, calls Gemini, streams response via Vercel Edge streaming. Writes `chat_messages` row after completion. Export: `export const config = { runtime: 'edge' }` + `export default async function handler(req: Request): Promise<Response> {...}`
- **Cloud Run `/stream` SSE endpoint**: wired from frontend via `EventSource` with `Authorization: Bearer <supabase_jwt>`. Cloud Run validates JWT against `SUPABASE_JWT_SECRET` (no Supabase call on hot path). Handles video intents ①③④.
- `cron-reset-processing` runs every 5 minutes (Foundation)

**Frontend scope:**
- `src/routes/_app/route.tsx` (ChatScreen) — port `ChatScreen.tsx` from Make
- `src/routes/_app/components/` — DiagnosisRow, HookRankingBar, BriefBlock, CreatorCard, ThumbnailStrip, CorpusCite, CreditBar, URLChip, ChatInput, StreamingStatusText, FreeQueryPill, PromptCards, ScrollDownPill, CopyButton

**Make files to port** (from `src/make-import/src/app/screens/`)**:**
- `ChatScreen.tsx` → `src/routes/_app/route.tsx`

**Data hooks needed:**
- `useChatSession(sessionId)` — `src/hooks/useChatSession.ts`
- `useChatSessions()` — `src/hooks/useChatSessions.ts`
- `useProfile()` — Realtime subscription to `profiles` for live CreditBar updates

**Intent routing (client-side rule-based, first pass):**
- URL present → Intent ①③④ (video) → SSE to `VITE_CLOUD_RUN_API_URL/stream`
- Handle present (`@`) → Intent ③ (competitor profile) or ④ (Soi Kênh if own handle)
- "hot", "trending", "xu hướng", "đang lên" without URL → Intent ⑥ (trend spike) — free
- "tìm KOL", "KOL", "tìm creator" → Intent ⑦ (find creators) — free
- Follow-up (has previous assistant message in session) → Intent follow_up — free
- Default text → Intent ⑤ (brief generation) or ⑥ per keywords

**SSE streaming client pattern:**

`EventSource` does not support custom headers in browsers — do NOT pass the JWT as a query param (exposes it in server logs). Use `fetch()` with `Authorization: Bearer` and stream the response body as NDJSON/SSE:

```typescript
// src/hooks/useChatStream.ts
const response = await fetch(`${VITE_CLOUD_RUN_API_URL}/stream`, {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${supabaseSession.access_token}`,
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({ session_id: sessionId, query, intent_type }),
})

const reader = response.body!.getReader()
const decoder = new TextDecoder()
while (true) {
  const { done, value } = await reader.read()
  if (done) break
  const lines = decoder.decode(value).split('\n').filter(Boolean)
  for (const line of lines) {
    if (!line.startsWith('data: ')) continue
    const token: SSEToken = JSON.parse(line.slice(6))
    // append token.delta to message buffer
    // if token.done → finalize message
  }
}
```

Cloud Run `/stream` endpoint validates JWT via `jsonwebtoken.verify(token, SUPABASE_JWT_SECRET)` — stateless, no Supabase call on hot path. For reconnection, include `stream_id` + `last_seq` in the POST body; Cloud Run replays from its 60s in-memory buffer.

**Dopamine moments:**
- D1 (DiagnosisReveal): `DiagnosisRow` items stagger-in from left, 150ms each, on mount
- D2 (HookRankingBars): animated width 0→%, 400ms cubic, on mount
- D3 (BriefDelivered): BriefBlock slides in sequentially
- D4 (CreatorCardsFound): CreatorCards appear with fade + subtle translate-y
- D5 (CreditConsumption): CreditBar border flash `--purple` 200ms → "−1" ghost floats up opacity 1→0, translateY 0→-12px, 400ms
- D6 (FreeQueryConfirmation): FreeQueryPill fade in 120ms → linger 2s → fade out 200ms

**Copy slots (key):**
- `empty_greeting`: "Sẵn sàng phân tích content của bạn."
- `prompt_card_1`: "Tại sao video này ít view — lỗi ở đâu?"
- `credit_bar_zero`: "Hết credit. Mua thêm →"
- `paywall_inline`: "Hết deep credit tháng này. Mua thêm 10 credit = 130.000 VND."
- `loading_diagnosis_1`: "Đang tải video..."
- `loading_diagnosis_2`: "Đang xem video của bạn..."
- `loading_diagnosis_3`: "Đang so sánh với {{count}} video trong niche..."
- `error_video_fail`: "Video không tải được — thử dán lại hoặc dùng video khác."
- `stream_interrupted`: "— Bị gián đoạn. Gõ 'tiếp' để tiếp tục."

**Credit cost:** 1 deep credit for intents ①–⑤. Free for ⑥, ⑦, follow-ups.

**Acceptance criteria:**
- [ ] Paste TikTok URL → streaming response appears token-by-token
- [ ] DiagnosisRows render with D1 stagger animation
- [ ] CreditBar decrements with D5 animation after deep query
- [ ] Free intent (trend, KOL search) → FreeQueryPill appears (D6)
- [ ] Credits = 0 → inline paywall appears, no query sent
- [ ] `is_processing` set true on query start, cleared on completion/error
- [ ] SSE reconnect: disconnect mid-stream → reconnect with stream_id+seq → replay from buffer

---

### Feature: history

**Backend scope:**
- Reads `chat_sessions` + `chat_messages` (already RLS-scoped in Foundation)
- `searchSessions(query)` — Postgres text search on `first_message`
- `deleteSession(sessionId)` — sets `deleted_at` (soft delete)

**Frontend scope:**
- `src/routes/_app/history/route.tsx` — port `HistoryScreen.tsx` from Make
- `useChatSessions()` hook (built in chat-core — reused)
- `searchSessions(query)` in `src/lib/data/sessions.ts`

**Make files to port** (from `src/make-import/src/app/screens/`)**:**
- `HistoryScreen.tsx` → `src/routes/_app/history/route.tsx`

**Copy slots (key):**
- `empty_history`: "Chưa có phiên nào. Dán link TikTok hoặc hỏi câu đầu tiên để bắt đầu."
- `intent_badge_diagnosis`: "Soi Video"
- `intent_badge_trends`: "Xu hướng"
- `intent_badge_creator`: "Tìm KOL"
- `intent_badge_brief`: "Brief"
- `intent_badge_competitor`: "Đối thủ"
- `intent_badge_soikenh`: "Soi Kênh"

**Credit cost:** N/A (free browsing)

**Acceptance criteria:**
- [ ] Sessions render sorted by `created_at DESC`
- [ ] Search filters session list as user types
- [ ] Tap session → ChatScreen resumes with full message history
- [ ] Soft delete removes session from list (deleted_at IS NOT NULL filtered by RLS)

---

## Wave 2 — Feature Context Packages

### Feature: explore

**Backend scope:**
- `video_corpus` table + RLS (`auth.uid() IS NOT NULL` for SELECT) — from Foundation migration 005
- `useVideoCorpus(filters)` — `src/hooks/useVideoCorpus.ts`: paginated 20-item pages, filters by `niche_id`, `indexed_at` range, sort by views/ER/date
- `useVideoDetail(videoId)` — `src/hooks/useVideoDetail.ts`
- `getRelatedVideos(videoId, nicheId)` — `src/lib/data/corpus.ts`: SELECT top-5 by ER from same niche

**Frontend scope:**
- `src/routes/_app/explore/route.tsx` — ExploreScreen (NO Make file — implement from screen-spec)
- Components: ExploreGrid (2-col), VideoCard, NicheFilterRow, DateRangeFilter, SortSelector, VideoDetailModal (sheet modal with `<video>` player), BreakoutHitsSidebar (desktop only)

**Key implementation notes:**
- `video_corpus.video_url` → R2 public URL → `<video src={video_url} muted autoPlay playsInline />` in modal
- `video_corpus.thumbnail_url` → R2 WebP → lazy loaded `<img>` in grid
- "Phân tích video này" CTA: `navigate('/app', { state: { prefillUrl: tiktok_url } })`
- Infinite scroll: TanStack Query `useInfiniteQuery` with `fetchNextPage` on bottom of grid

**Copy slots (key):**
- `explore_header`: "Khám phá {{N}} video"
- `explore_filter_all`: "Tất cả"
- `video_detail_cta`: "Phân tích video này"
- `empty_explore`: "Không có video nào trong khoảng này — thử bỏ bộ lọc."

**Credit cost:** Free (0 credits)

**Acceptance criteria:**
- [ ] 2-col grid renders VideoCards with thumbnails
- [ ] Niche filter chip updates grid (no full page reload)
- [ ] Tap VideoCard → VideoDetailModal slides up, video autoplays muted
- [ ] Mute/unmute toggles audio
- [ ] "Phân tích video này" → navigates to ChatScreen with URL pre-filled
- [ ] Infinite scroll loads next page when user reaches bottom

---

### Feature: trends

**Backend scope:**
- `niche_intelligence` materialized view + `trend_velocity` + `hook_effectiveness` + `format_lifecycle` + `creator_velocity` tables — from Foundation migration 005
- `useNicheIntelligence(nicheId)` — `src/hooks/useNicheIntelligence.ts`
- `useTrendVelocity(nicheId)` — `src/hooks/useTrendVelocity.ts`
- `useHookEffectiveness(nicheId)` — `src/hooks/useHookEffectiveness.ts`
- `useFormatLifecycle(nicheId)` — `src/hooks/useFormatLifecycle.ts`

**Frontend scope:**
- `src/routes/_app/trends/route.tsx` — TrendScreen (from Make: `TrendScreen.tsx`)
- Components: NicheSelector, TrendHeader, HookRankingSection, HookRankingBar (D2), CorpusCite, FormatLifecycleSection, TrendingKeywordSection, ThumbnailStrip

**Make files to port** (from `src/make-import/src/app/screens/`)**:**
- `TrendScreen.tsx` → `src/routes/_app/trends/route.tsx`

**Dopamine moment:**
- D2: HookRankingBars animate width 0→% on mount and on niche change. Top bar purple, others lighter gray. Multiplier fades in at bar end. 400ms cubic, 100ms stagger.

**Copy slots (key):**
- `section_hook_ranking`: "Hook đang chạy trong {{niche}}"
- `corpus_cite_trend`: "{{count}} video · 7 ngày · Updated {{hours}}h ago"
- `section_format_rising`: "Format đang lên"
- `section_format_falling`: "Format đang giảm"
- `empty_trend`: "Chưa có đủ data cho {{niche}} tuần này. Thử xem xu hướng của Review đồ gia dụng — niche có data đầy đủ nhất."
- `thumbnail_action`: "Phân tích video này"

**Edge cases:**
- Batch job data > 36h old → CorpusCite shows warning "Data cũ hơn 36 tiếng — đang cập nhật."
- Niche has < 10 videos indexed → empty state with redirect to best-data niche

**Credit cost:** Free (Intent ⑥ Trend Spike is unlimited)

**Acceptance criteria:**
- [ ] NicheSelector shows all 17 niches; user's primary_niche pre-selected
- [ ] HookRankingBars animate on mount (D2) and on niche chip tap
- [ ] FormatLifecycleSection shows rising/falling formats
- [ ] ThumbnailStrip horizontal scroll with "Phân tích video này" deep link

---

### Feature: billing

**Backend scope:**
- `subscriptions` + `credit_transactions` + `processed_webhook_events` tables — Foundation migration 004
- `create-payment` Edge Function: validate JWT + plan, call PayOS REST, create `subscriptions` row (status=pending), return QR/bank details
- `payos-webhook` Edge Function: HMAC verify → idempotency insert → activate subscription → `decrement_credit` RPC (inverted, grant credits) → send-email receipt
- `send-email` Edge Function: receipt template
- `useSubscription()` — `src/hooks/useSubscription.ts`

**Frontend scope:**
- `src/routes/_app/pricing/route.tsx` — PricingScreen (from Make: `PricingScreen.tsx`)
- `src/routes/_app/checkout/route.tsx` — CheckoutScreen (from Make: `CheckoutScreen.tsx`)
- `src/routes/_app/payment-success/route.tsx` — PaymentSuccessScreen (from Make: `PaymentSuccessScreen.tsx`)

**Make files to port** (from `src/make-import/src/app/screens/`)**:**
- `PricingScreen.tsx` → `src/routes/_app/pricing/route.tsx`
- `CheckoutScreen.tsx` → `src/routes/_app/checkout/route.tsx`
- `PaymentSuccessScreen.tsx` → `src/routes/_app/payment-success/route.tsx`

**Phase 4 flags (implement during this feature):**
- **N1 (PaymentSuccessScreen):** Data via React Router `state` — `navigate('/app/payment-success', { state: { planName, creditsDelta } })`. On refresh: state is lost → fall back to Supabase profiles fetch, show "Credits đã được cập nhật." without plan name. Do NOT use query params.
- **N2 (CheckoutScreen):** Payment method pre-selection: monthly/6-month → MoMo pre-selected; annual → bank transfer pre-selected. Applied at runtime, not in Make layout.

**Payment method pre-selection logic:**
```typescript
const defaultMethod = billingPeriod === 'annual' || billingPeriod === 'biannual'
  ? 'bank_transfer'
  : 'momo'
```

**Overage packs (3 packs per Make):**
| Pack | Price | Per credit |
|---|---|---|
| 10 credits | 130,000đ | 13,000đ |
| 30 credits | 350,000đ | 11,700đ |
| 50 credits | 550,000đ | 11,000đ — "Phổ biến" badge |

**Copy slots (key):**
- `screen_title`: "Chọn gói phù hợp"
- `tier_starter_price_monthly`: "249.000đ/tháng"
- `tier_starter_price_annual`: "199.000đ/tháng · thanh toán cả năm"
- `overage_50`: "50 lần phân tích sâu — 550.000đ (11.000đ/lần) · Phổ biến"
- `screen_heading` (PaymentSuccess): "Đã thêm {{count}} deep credits."
- `cta_button` (PaymentSuccess): "Bắt đầu phân tích ngay"

**Dopamine moment:**
- PaymentSuccessScreen: CreditBar credit count increments from old → new balance over 800ms (upward D5 variant)

**Credit cost:** N/A

**Acceptance criteria:**
- [ ] PricingScreen annual pre-selected on load, Starter highlighted
- [ ] Billing toggle switches prices instantly
- [ ] CheckoutScreen: MoMo pre-selected for monthly, bank transfer for annual
- [ ] PayOS webhook fires → credits update in Supabase → CreditBar live-updates (Realtime)
- [ ] Duplicate webhook retry → second request returns 200 without double-crediting
- [ ] PaymentSuccessScreen shows new credit count
- [ ] Receipt email sent via Resend

---

### Feature: settings

**Backend scope:**
- `updateProfile(patch)` — `src/lib/data/profile.ts`: `supabase.from('profiles').update({ primary_niche, niche_id })...`
- `credit_transactions` SELECT (via useProfile or dedicated hook)

**Frontend scope:**
- `src/routes/_app/settings/route.tsx` — SettingsScreen (from Make: `SettingsScreen.tsx`)
- `src/routes/_app/learn-more/route.tsx` — LearnMoreScreen (from Make: `LearnMoreScreen.tsx`)

**Make files to port** (from `src/make-import/src/app/screens/`)**:**
- `SettingsScreen.tsx` → `src/routes/_app/settings/route.tsx`
- `LearnMoreScreen.tsx` → `src/routes/_app/learn-more/route.tsx`

**Copy slots (key):**
- `subscription_tier_label`: "Gói {{tier_name}}"
- `subscription_credits`: "{{count}} deep credits còn lại"
- `subscription_expiry`: "Credits hết hạn: {{expiry_date}}"
- `logout_confirm`: "Đăng xuất khỏi GetViews?"

**Edge cases:**
- Free tier: SubscriptionSection shows "10 lần phân tích sâu miễn phí (lifetime)" with no expiry
- Expired pack: `credits_reset_at` in past → "Gói đã hết hạn — gia hạn để tiếp tục phân tích sâu."

**Credit cost:** N/A

**Acceptance criteria:**
- [ ] SettingsScreen loads profile data (name, email, tier, credits, expiry)
- [ ] Niche change saves to `profiles.primary_niche` → ChatScreen NicheBadge updates
- [ ] Logout clears Supabase session → redirects to `/login`
- [ ] CreditHistoryList shows last 20 credit events
- [ ] LearnMoreScreen renders all sections with working external links

---

### Feature: email-cron (backend only)

**Backend scope — all Edge Functions:**

| Function | Schedule | Action |
|---|---|---|
| `cron-expiry-check` | `0 2 * * *` UTC (9AM ICT) | Find active subs expiring ≤7d → send reminder emails; expire subs past `expires_at` → downgrade profiles |
| `cron-reset-free-queries` | `0 17 * * *` UTC (midnight ICT) | `UPDATE profiles SET daily_free_query_count = 0` for all users |
| `cron-prune-webhooks` | `0 20 * * 0` UTC (Sunday 3AM ICT) | DELETE `processed_webhook_events` older than 30 days |
| `cron-reset-processing` | `*/5 * * * *` UTC (every 5 min) | Reset `is_processing = true` on profiles where `updated_at < now() - 5 min` |

**Email templates (in `send-email` Edge Function):**
- `expiry_reminder_7d`: Subject "Gói GetViews của bạn hết hạn trong 7 ngày"
- `expiry_reminder_3d`: Subject "Gói GetViews của bạn hết hạn trong 3 ngày"
- `expiry_reminder_1d`: Subject "Gói GetViews của bạn hết hạn ngày mai"

**Acceptance criteria:**
- [ ] `supabase functions invoke cron-expiry-check` runs without errors in dev
- [ ] Expiring subscription → reminder email sent (verify in Resend dashboard)
- [ ] Expired subscription → profile downgraded to free tier
- [ ] `daily_free_query_count` resets to 0 after cron run

---

## SEO & PWA Checklist (Foundation deliverable)

| Item | File | Status |
|---|---|---|
| Pre-rendered landing page | `react-router.config.ts` `prerender: ['/']` | Verify |
| OG image | `public/og-image.png` (1200×630) | Human to create |
| PWA icons | `public/icons/icon-192.png`, `icon-192-maskable.png`, `icon-512.png`, `icon-512-maskable.png` | Human to create |
| PWA screenshot | `public/screenshots/chat-mobile.png` (390×844) | Human to create |
| Sitemap | `public/sitemap.xml` | Backend Developer creates |
| Robots | `public/robots.txt` | Backend Developer creates |
| Manifest | `public/manifest.json` | Already exists — verify contents |
| Service worker | `vite-plugin-pwa` in `vite.config.ts` | Already configured |
| JSON-LD | Landing page component | Frontend Developer adds |
| `html lang="vi"` | `src/root.tsx` | Frontend Developer adds |

---

## Notes for Agent Dispatch

- **Make import flow:** For each Make screen, copy the TSX content into the route file, then wire up: (1) replace `mockProfile`/`mockMessages` etc. with real TanStack Query hooks, (2) add loading/error/empty states using the screen-spec States section, (3) add navigation using the screen-spec Navigation section, (4) add dopamine animations using the screen-spec Dopamine section.
- **ExploreScreen has NO Make file** — implement directly from screen-spec. Components can reuse NicheFilterRow from TrendScreen and VideoCard patterns from TrendScreen's explore tab section.
- **Cloud Run wiring:** Frontend just opens an `EventSource` to `VITE_CLOUD_RUN_API_URL/stream` with the Supabase JWT. The Cloud Run Python service (already in `cloud-run/`) handles the rest.
- **Vercel Edge Runtime:** `/api/chat` is a Vercel Edge Function at `api/chat.ts` (project root — NOT inside `src/`). React Router v7 runs in `ssr: false` SPA mode and cannot serve server routes. The Vercel `api/` directory is picked up automatically by Vercel's file-based routing. Use `export const config = { runtime: 'edge' }` + `export default async function handler(req: Request): Promise<Response>`. Validates JWT via Supabase `auth.getUser()`, calls Gemini via `@google/generative-ai`, streams back via `ReadableStream`.
- **Realtime CreditBar:** `useProfile()` subscribes to `profiles` Supabase Realtime channel — balance updates live when webhook credits the account. No polling needed.
- **ZaloPay:** Figma Make PricingScreen includes ZaloPay in PaymentMethodRow. Add to CheckoutScreen UI but gate behind a `VITE_ZALOPAY_ENABLED=true` flag until PayOS integration is confirmed.
