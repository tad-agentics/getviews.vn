# idea-directions.jsx — Phase C.0.2 decision

**Decision:** **Option 3 — Re-scope from `answer.jsx`** (default per phase-c-plan.md).

**Rationale:** `artifacts/uiux-reference/screens/idea-directions.jsx` is not present in-tree. Rather than block C.3, `IdeaBlock`, `StyleCard`, and `StopRow` will be composed from `AnswerBlock` + shared primitives in `answer.jsx` + §C.3 spec.

**Impact on C.3:** Base estimate **1.5 weeks** applies; no +1 week design commission.

**Review by:** End of C.0 spike week.
