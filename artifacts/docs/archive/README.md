# Archived docs

Place **superseded** specifications, audits, or runbooks here so the root of `artifacts/docs/` stays easy to scan.

**Convention:** When moving a file in, add one line at the top:

`> **Archived** YYYY-MM-DD — replaced by / reason: …`

Do not delete historically useful audits without team agreement; archiving preserves git blame and links from older commits.
