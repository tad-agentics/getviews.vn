---
description: Project context — GetViews.vn
alwaysApply: true
---

## Project

App: GetViews.vn
Deployment mode: pwa
Stack: React Router v7 (Vite) · Supabase (DB + Auth + RLS + Edge Functions) · TanStack React Query · Vercel · Tailwind CSS · Studio UIUX reference (`artifacts/uiux-reference/`)

## Architecture summary

- **Frontend:** React SPA deployed on Vercel. Pre-rendered landing page at `/` for SEO. All `/app/*` routes are client-side SPA behind auth guard (`src/routes/_app/layout.tsx`).
- **Backend:** Supabase is the sole backend. Frontend talks to Supabase directly via publishable key + JWT. RLS handles all authorization. No API routes in the frontend.
- **Server logic — Edge Functions:** Webhooks (PayOS payment confirmation), cron (expiry check, free query reset, webhook prune, stale processing guard), email (Resend via `send-email` Edge Function), PayOS payment creation.
- **Server logic — Cloud Run (Python):** Video analysis pipeline (Intents ①③④ — avoids Vercel 60s timeout). Streams SSE directly to client. JWT-validated against `SUPABASE_JWT_SECRET`. Two services: user-facing (`min-instances: 1`, `/stream` SSE endpoint + `/channel/diagnose` SSE endpoint) + batch (`min-instances: 0`, daily 4 AM ICT indexer).
  - `/channel/diagnose` is the **narrative** channel endpoint (Lightreel-style, 3 credits, 7-day cache, live EnsembleData + corpus hybrid). Legacy `/channel/analyze` was **removed** (migration `20260715000001`).
- **Text intents:** Intents ⑤⑥⑦ + follow-ups route through Vercel Edge Runtime (`/api/chat`). Gemini API call, stream via Vercel Edge.
- **Design:** Tracked reference in `artifacts/uiux-reference/` (shell + screens + tokens file). Production screens live under `src/routes/` with hooks, auth, and states. Optional gitignored `src/make-import/` only if pasting a legacy Figma Make dump.

## State management

TanStack Query = server/remote state. `useState` = local UI state. React Context = low-frequency shared state (auth, Supabase client). **No Zustand, no Redux, no Jotai, no Recoil.**

## What this app does

GetViews.vn is a Vietnamese-language TikTok creative intelligence platform for Vietnamese creators and agencies. Users paste a TikTok URL or ask a question in chat — GetViews analyzes the video frame-by-frame using Gemini vision and compares it against a pre-indexed corpus of 46,000+ analyzed Vietnamese TikTok videos, returning a specific diagnosis, hook effectiveness rankings, and actionable fixes in Vietnamese within 20-30 seconds.

Primary user: Minh (24, Shopee affiliate creator) who needs to know why his video flopped and what to film next. Secondary user: Linh (28, agency Creative Lead) who needs creator discovery and production-ready KOL briefs.

## Not building (out of scope)

- Native iOS/Android (Expo) — web/PWA only; the `mobile/` workspace was removed from this repo
- English-language support — Vietnamese-only product; global users use GetReels
- MCP server access — Vietnamese developer market too small for v1
- Multi-platform (Reels, Shorts) — TikTok only
- Creator marketplace / talent management (contracts, payments, ongoing relationships)
- Video editing tools — GetViews diagnoses, not edits
- Scheduling / posting
- Shopee analytics / affiliate dashboard — Kalodata territory
- Notification management screen — email-based renewal reminders only
- Admin dashboard — use Supabase Dashboard directly
- Competitor tracking dashboard screen — managed as chat sessions (Wave 2)
- Monday weekly email — Wave 2 (cron + template built, content generation deferred)
- Recurring subscription billing — PayOS is one-time; packs expire and renew manually
- Zalo notifications — Wave 2
- Douyin trend forecasting — Wave 2
- Full livestream analysis — Wave 3+
- OnboardingScreen — dropped in Figma phase; niche set inline on first ChatScreen session

## LLM rules

LLM features are used. Models:
- `gemini-3.1-flash-lite` — **universal default** for all pipeline paths: video frame extraction, knowledge answers, batch indexing, Vietnamese synthesis, intent classification, follow-up text responses (Cloud Run Python + Vercel Edge Runtime). GA stable as of 2026-05-07. Pricing: $0.25/M in, $1.50/M out.
- `gemini-3.1-flash` — optional upgrade for synthesis quality; set via `GEMINI_SYNTHESIS_MODEL` env override. Pricing: $0.50/M in, $3.00/M out.

Cost ceiling: **~$80–90/month** across Gemini usage (batch + user queries) plus optional **HI-14** GCP Speech-to-Text on **video** paths only (carousels skip STT). Optional **HI-13** Gemini Batch API on nightly video shards reduces per-token cost when enabled.

Never reference Gemini 2.5 (shutdown June 17, 2026) or Gemini 2.0 (shutdown March 31, 2026). Use Gemini 3.x only.

**Supplemental ASR (HI-14):** Google Cloud Speech-to-Text `vi-VN` may run server-side before Gemini vision on **video file** extractions; transcripts are cached in `vietnamese_asr_cache`. Not used for carousels (HI-17).

AI inference location:
- Video analysis (①③④): Cloud Run Python service — `GEMINI_API_KEY` in Cloud Run env only
- Text intents (⑤⑥⑦ + follow-ups): Vercel Edge Runtime — `GEMINI_API_KEY` in Vercel env secret only
- Batch indexing: Cloud Run batch service
- API keys never in frontend bundle

## AI module boundary

- All AI wrapper modules for the frontend live in `src/lib/` (e.g., `src/lib/niche-resolver.ts`)
- Components and hooks never call AI SDKs directly — always through wrapper modules or via Supabase Edge Function / Cloud Run SSE endpoint
- Cloud Run Python pipeline lives in `cloud-run/getviews_pipeline/` — forked from GetReels-MCP, adapted for Vietnamese context and Supabase corpus
- AI modules receive niche, corpus data, and user context as function arguments — never import app-specific config
- Frontend → Cloud Run: direct SSE with Supabase JWT in Authorization header
- Frontend → Vercel Edge: `/api/chat` endpoint (text intents only)

## Key constraints

- All Gemini API keys are server-only — never in the frontend bundle (`VITE_` prefix forbidden for Gemini keys)
- Atomic credit deduction via Supabase RPC `decrement_credit()` — WHERE guard prevents overdraft (TD-1)
- PayOS webhook idempotency via `processed_webhook_events` UNIQUE constraint — retries safe (TD-2)
- Concurrent request guard: `profiles.is_processing` boolean — cron resets stale flags after 5 minutes (TD-3)
- SSE reconnection: `stream_id` + `seq` per token — Cloud Run replays from 60s in-memory buffer (TD-4)
- Credits granted upfront at PAID webhook — no monthly top-up cron (TD-5). PayOS is one-time.
- **TD-6:** With `NICHE_RESOLVER_MODE=route`, promoted `content_class_id` only when junction lookup succeeds; otherwise hashtag ladder — taxonomy seed must stay aligned (see `system-design.md` §10).
- **TD-7:** Live SSE and batch ingest share the same extraction + HI-9 classification contract — no forked prompts without parity audit (`system-design.md` §10).
- Facebook OAuth is non-negotiable for the Vietnamese market — do not remove or deprioritize
- Vietnamese language throughout all UI copy — no English strings in user-facing UI
- Mobile-first viewport: 360–393px baseline, 44×44px minimum touch targets
- R2 single bucket (`getviews-frames` — set via `R2_BUCKET_NAME`) is public — no signed URLs needed (public TikTok content thumbnails/clips). Five key namespaces: `frames/`, `thumbnails/`, `videos/`, `video_shots/`. Dedup invariant: at most one extension per video_id under `thumbnails/`. Janitor cron runs Sundays 18:00 UTC (`cron-batch-r2-janitor`). Full architecture in `artifacts/docs/system-design.md §15`.
- Chat messages are immutable — no UPDATE on `chat_messages`; use soft-delete on sessions via `deleted_at`
- `video_corpus` INSERT is batch-only (service role via Cloud Run) — RLS blocks client writes
- ZaloPay in Figma Make PricingScreen — confirm PayOS integration supports it before launching

---

## Builder Ethos (all agents)

All agents follow the principles in `artifacts/docs/ETHOS.md`:
1. **Completeness Is Cheap** — always recommend the complete option. The delta is minutes with AI. Include `Completeness: X/10` when presenting options.
2. **Search Before Building** — check if Supabase, React Router, TanStack, or Tailwind has a built-in before rolling custom. Three layers: tried-and-true, new-and-popular, first-principles.
3. **User Sovereignty** — agents recommend, the human decides. No agent self-proceeds past a blocking gate.

---

## AskUserQuestion Format (all agents)

Every question presented to the Tech Lead (or from the Tech Lead to the human) must follow this structure:

1. **Re-ground:** State the project, current branch (`git branch --show-current`), and current task. 1–2 sentences. Assume the reader hasn't looked at this window in 20 minutes.
2. **Simplify:** Explain the problem in plain English. No raw function names, no internal jargon. Use concrete examples. Say what it DOES, not what it's called.
3. **Recommend:** `RECOMMENDATION: Choose [X] because [one-line reason]`. Include `Completeness: X/10` for each option. Calibration: 10 = complete implementation (all edge cases, full coverage), 7 = happy path only, 3 = shortcut that defers significant work.
4. **Options:** Lettered: `A) ... B) ... C) ...`. When an option involves effort, show both scales: `(human: ~X / AI-assisted: ~Y)`.

---

## Completion Status Protocol (all agents)

Every agent must end its dispatch with one of these status signals:

- **DONE** — All steps completed successfully. Evidence provided for each claim.
- **DONE_WITH_CONCERNS** — Completed, but with issues the Tech Lead should know about. List each concern.
- **BLOCKED** — Cannot proceed. State what is blocking and what was tried.
- **NEEDS_CONTEXT** — Missing information required to continue. State exactly what is needed.

### Escalation rule

It is always OK to stop and say "this is too hard" or "I'm not confident in this result." Bad work is worse than no work.

- If you have attempted a task **3 times** without success: STOP and escalate.
- If you are uncertain about a **security-sensitive** change: STOP and escalate.
- If the scope of work exceeds what you can **verify**: STOP and escalate.

Escalation format:

```
STATUS: BLOCKED | NEEDS_CONTEXT
REASON: [1–2 sentences]
ATTEMPTED: [what you tried]
RECOMMENDATION: [what the Tech Lead / human should do next]
```
